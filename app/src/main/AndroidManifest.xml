<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- miuix-blur 声明的 minSdk 高于本应用（26）：运行时按 SDK_INT >= 33 判断，低版本自动降级 -->
    <uses-sdk tools:overrideLibrary="top.yukonga.miuix.kmp.blur" />

    <!-- 调用本地 C++ 后端（POST /api/v1/execute，SSE） -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.CoverRootApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.ThemeSwitchDemo.Starting"
        tools:targetApi="35">

        <!--
          真正的业务入口：
          - 全程保持 enable，绝不允许 pm disable
          - exported=true，不带任何 MAIN/LAUNCHER 过滤器
          - 图标隐藏后由命令行唤醒：am start -n com.demo.themeswitch/.MainActivity
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ThemeSwitchDemo" />

        <!--
          桌面启动入口（别名），MAIN + LAUNCHER 只挂在这里。
          HyperOS 适配：显式声明 icon/roundIcon/label，避免 alias 被 disable 后，
          MIUI Launcher 回退用 application 默认资源自动合成一个图标残留在桌面。
        -->
        <activity-alias
            android:name=".LauncherAlias"
            android:targetActivity=".MainActivity"
            android:enabled="true"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>
    </application>

</manifest>
